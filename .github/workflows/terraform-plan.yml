name: terraform plan

on:
  pull_request:
    paths:
      - "envs/**"
      - "modules/**"
      - ".github/workflows/terraform-*.yml"

permissions:
  id-token: write # required for OIDC
  contents: read
  pull-requests: write # to comment the plan

env:
  TF_VERSION: "1.10.0" # must be >= 1.10 -- see envs/prod/providers.tf
  AWS_REGION: ap-southeast-7

jobs:
  plan:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: envs/prod


    env:
      # terraform.tfvars is gitignored, so values come from GitHub repository
      # variables (Settings > Secrets and variables > Actions > Variables).
      TF_VAR_app_domain: ${{ vars.APP_DOMAIN }}
      TF_VAR_admin_email: ${{ vars.ADMIN_EMAIL }}
      TF_VAR_admin_cidr: ${{ vars.ADMIN_CIDR }}
      TF_VAR_enable_ssh: ${{ vars.ENABLE_SSH }}
      # GitHub withholds secrets and the OIDC token from pull_request runs
      # triggered by forks (by design, so a malicious fork PR can't exfiltrate
      # them) -- so a fork PR can never assume an AWS role here. Route those
      # runs to a backend-less validate instead of letting them fail on
      # "Configure AWS credentials" with a confusing error.
      IS_FORK: ${{ github.event.pull_request.head.repo.full_name != github.repository }}

    steps:
      - uses: actions/checkout@v4

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Format check
        run: terraform fmt -check -recursive ../..

      - name: Init and validate (fork PR -- no AWS access)
        if: env.IS_FORK == 'true'
        run: |
          echo "::notice::This PR is from a fork, so GitHub withholds the OIDC token and secrets needed to assume an AWS role here. Ran format check and a backend-less 'terraform validate' only -- a maintainer needs to review and re-plan before merging."
          terraform init -backend=false
          terraform validate

      - name: Configure AWS credentials
        if: env.IS_FORK != 'true'
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_PLAN_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Init
        if: env.IS_FORK != 'true'
        run: terraform init

      - name: Validate
        if: env.IS_FORK != 'true'
        run: terraform validate

      - name: Plan
        if: env.IS_FORK != 'true'
        id: plan
        run: terraform plan -no-color -input=false
        continue-on-error: true

      - name: Comment plan on PR
        if: env.IS_FORK != 'true'
        uses: actions/github-script@v7
        env:
          # Passed as an env var, not spliced into the script below -- the plan
          # text comes from the PR's own .tf changes, so treat it as untrusted.
          # Interpolating it directly into the JS template literal via ${{ }}
          # would let a crafted resource/variable name break out of the string
          # and run arbitrary code with this job's pull-requests:write token.
          PLAN_OUTCOME: ${{ steps.plan.outcome }}
          PLAN_STDOUT: ${{ steps.plan.outputs.stdout }}
        with:
          script: |
            const body = `#### Terraform Plan \`${process.env.PLAN_OUTCOME}\`

            <details><summary>Show plan</summary>

            \`\`\`terraform
            ${process.env.PLAN_STDOUT}
            \`\`\`

            </details>`;
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: body
            });

      - name: Fail if plan failed
        if: env.IS_FORK != 'true' && steps.plan.outcome == 'failure'
        run: exit 1
